{
  "name": "@stackday/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'API functions will be deployed to Vercel. Use pnpm vercel:dev to test locally.'",
    "build": "tsc",
    "test": "vitest",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "type-check": "tsc --noEmit",
    "vercel:dev": "vercel dev --yes"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vercel": "^32.0.0",
    "vitest": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
